def importar_json(dados):
    # Aqui você pode processar a importação dos dados JSON
    tarefas = dados  # Tarefas enviadas no corpo JSON
    # Lógica para salvar as tarefas no banco de dados ou na memória
    print(f"Tarefas importadas: {tarefas}")
